# Datasets

Public datasets are downloaded into `datasets/cache/` and are not committed.
The manifests record official sources, expected filenames, and evidence class.

`fetch_datasets.py` downloads source files only. It does not grant additional
rights; the upstream license and terms control use.

